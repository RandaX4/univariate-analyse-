df<-read.csv("C:/Users/EXTRA/Downloads/HR_comma_sep.csv")
dim(df)
summary(df)
str(df)
colSums(is.na(df)) 
mean(df$satisfaction_level) 
median(df$satisfaction_level) 
names(sort(-table(df$satisfaction_level)))[1]
min(df$satisfaction_level)
max(df$satisfaction_level)
quantile(df$satisfaction_level)
var(df$satisfaction_level)
sd(df$satisfaction_level)
table(df$satisfaction_level)
summary(df$satisfaction_level)
mean(df$last_evaluation)
median(df$last_evaluation)
mode(df$last_evaluation)
max(df$last_evaluation)
min(df$last_evaluation)
quantile(df$last_evaluation)
var(df$last_evaluation)
sd(df$last_evaluation)
table(df$last_evaluation)
